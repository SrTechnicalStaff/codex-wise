---
layout: default
title: Web Dashboard
nav_order: 7
---

# Web Dashboard

The local server exposes the REST API and optional browser UI.

```bash
codex-wise serve
codex-wise serve --no-ui
```

The API includes repository, page, graph, search, workspace, job, and generated `AGENTS.md` endpoints.
