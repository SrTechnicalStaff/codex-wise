# Server Package

FastAPI, MCP, webhook, and background-job package for Codex Wise.

Run through the CLI:

```bash
codex-wise serve
codex-wise mcp --transport stdio
```

Environment variables:

```bash
CODEX_WISE_API_KEY=...
CODEX_WISE_EMBEDDER=openai
CODEX_WISE_API_URL=http://localhost:7337
```
