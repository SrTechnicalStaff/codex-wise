# Core Package

Core indexing and analysis library for Codex Wise. It contains file traversal, AST parsing, dependency graph construction, generation models, persistence, provider adapters, dead-code analysis, workspace scanning, and cross-repo analysis.

Most users should enter through:

```bash
codex-wise init
codex-wise update
codex-wise doctor
```

The Python import namespace is `codex_wise`.
