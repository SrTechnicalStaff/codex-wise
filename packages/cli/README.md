# CLI Package

Command-line package for Codex Wise. The preferred executable is:

```bash
codex-wise --help
```

This package contains Click commands for initialization, update, status, search, MCP startup, server startup, workspace operations, and generated Codex instruction files.
