services:
  codex-wise:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "127.0.0.1:7337:7337"   # API — loopback only by default
      - "127.0.0.1:3000:3000"   # Web UI — loopback only by default
    volumes:
      # Mount the .codex-wise directory from your indexed repo
      - ${CODEX_WISE_DATA:-./data}:/data
      # Mount repo directory as read-only to prevent write-based attacks
      - type: bind
        source: ${REPO_PATH:?Set REPO_PATH to your repository directory}
        target: /repos
        read_only: true
    environment:
      - CODEX_WISE_DB_URL=sqlite+aiosqlite:////data/wiki.db
      - CODEX_WISE_EMBEDDER=${CODEX_WISE_EMBEDDER:-mock}
      - CODEX_WISE_HOST=0.0.0.0
      # SECURITY: Set this to enable API authentication (required for non-loopback)
      - CODEX_WISE_API_KEY=${CODEX_WISE_API_KEY:?Set CODEX_WISE_API_KEY for Docker deployments}
      # Set your LLM provider API key
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
    restart: unless-stopped
